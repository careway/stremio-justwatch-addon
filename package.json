{
  "name": "omnicatalogs",
  "version": "1.1.0",
  "description": "Every service. Every country. Select the catalogs you want and enjoy searching through them.",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --env-file=.env.development.local src/index.js",
    "test": "node --test test/**/*.test.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}

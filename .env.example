# Local development
PORT=7000
NODE_ENV=development

# Redis — local
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Redis — production (overrides REDIS_HOST/PORT when set)
# Use Upstash: https://console.upstash.com/
# REDIS_URL=rediss://default:<password>@<host>:<port>

{
  "name": "omnicatalogs",
  "version": "1.3.0",
  "description": "Every service catalog from a good bunch of countries. Select the catalogs you want and enjoy searching and filtering through them. You can install it as many times as catalogs from different countries you want.",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --env-file=.env.development.local src/index.js",
    "test": "node --test test/**/*.test.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "@vercel/analytics": "^2.0.1",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
